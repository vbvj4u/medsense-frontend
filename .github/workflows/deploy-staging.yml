name: Deploy - staging

# Scaffold only - see medsense-infra's terraform-staging.yml. Wire up
# a "staging" GitHub Environment with real secrets once that AWS
# account/environment exists.

on:
  workflow_dispatch: {}

permissions:
  id-token: write
  contents: read
  
jobs:
  deploy:
    uses: ./.github/workflows/_deploy-reusable.yml
    with:
      environment: staging
    secrets: inherit
