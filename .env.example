# Base URL of the backend API Gateway (medsense-infra `api_endpoint` output).
# Vite only exposes vars prefixed VITE_ to client code.
VITE_API_BASE_URL=https://replace-me.execute-api.ap-southeast-2.amazonaws.com
