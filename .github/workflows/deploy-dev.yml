name: Deploy - dev

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-reusable.yml
    with:
      environment: dev
    secrets: inherit
